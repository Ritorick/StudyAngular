import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ryuut.helloworld',
  appName: 'Hello World App',
  webDir: 'dist/hello-world-app/browser'
};

export default config;
